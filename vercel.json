{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "forceNpmInstall": true
      }
    }
  ],
  "installCommand": "npm install --force",
  "buildCommand": "npm run vercel-build",
  "framework": "nextjs",
  "regions": ["iad1"]
}